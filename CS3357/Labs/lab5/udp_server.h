#ifndef UDP_SERVER_H
#define UDP_SERVER_H

#include "udp_sockets.h"

int bind_socket(struct addrinfo*);
int create_server_socket(char*);
int bind_socket(struct addrinfo*);
int create_server_socket(char*);

#endif