/*
 * q8.c
 *
 *  Created on: Dec 6, 2013
 *      Author: ZiZo
 */

#include<stdio.h>

#define CHECK(x, y, n) (((x)>0&&(y)>0&&(x)<((n)-1)&&(y)<((n)-1)) ? 1 : 0)

int main(void)
{
	printf("%d", CHECK(1, 2, 4));
	return 0;
}
